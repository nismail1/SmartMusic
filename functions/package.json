{
  "name": "smartmusic-functions",
  "private": true,
  "main": "index.js",
  "engines": {
    "node": "20"
  },
  "scripts": {
    "serve": "npx firebase-tools@latest emulators:start --only functions",
    "deploy": "npx firebase-tools@latest deploy --only functions",
    "logs": "npx firebase-tools@latest functions:log"
  },
  "dependencies": {
    "firebase-admin": "^13.5.0",
    "firebase-functions": "^6.6.0"
  }
}
